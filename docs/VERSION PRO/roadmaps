###### 14 JUILLET 2026 

Étape 0 — Figer l'architecture (celle qu'on est en train de faire)

Objectif :

schéma des tables ;
règles métier ;
flux utilisateur ;
décisions V1/V2.

Temps :
2 à 4 heures

Ça paraît beaucoup pour "du papier", mais c'est ce qui évite de réécrire 3 fois le code.

Livrable :
un document de référence :

gestion-presences V1
====================

Groupes
Apprenants
Groupe_apprenants
Sessions
Session_apprenants
Présences
règles :
une session ouverte maximum par groupe ;
pas de session globale unique ;
une présence = session + apprenant ;
etc.
Étape 1 — Refaire la base Supabase

Créer les tables proprement :

groupes
groupe_apprenants
sessions (adaptée)
session_apprenants

Garder :

apprenants
presences

Modifier :

suppression de la logique active

Temps :
2 à 3 heures

Étape 2 — Adapter le backend

Principalement :

POST /sessions

Avant :

Créer une session

Après :

Recevoir groupe_id

Vérifier :
une session ouverte existe-t-elle ?

Créer session

Copier les apprenants du groupe
dans session_apprenants

Temps :
3 à 5 heures

POST /api/presences

Ajouter :

vérifier session ;
vérifier expiration ;
vérifier que l'apprenant appartient à cette session ;
enregistrer.

Temps :
2 à 4 heures

Étape 3 — Frontend boucle complète

Le vrai parcours :

Formateur :

choisir groupe
↓
démarrer séance
↓
QR affiché

Apprenant :

scanner
↓
confirmation

Formateur :

voir présents

Temps :
4 à 8 heures

Étape 4 — Tests

Là on garde ce qu'on a appris.

Tests utiles :

deux groupes simultanés OK ;
deux sessions même groupe refusées ;
mauvais QR refusé ;
doublon refusé ;
session terminée refusée.

Temps :
3 à 5 heures

Donc estimation globale réaliste

Si on travaille comme maintenant :

Version propre qui tourne :

15 à 25 heures

Si tu pouvais faire ça sans interruption avec un développeur à côté :

2 à 4 jours

Mais dans notre cas, il faut compter :

tes questions ;
mes explications ;
les essais ;
les petits détours utiles.

Donc je dirais :

Une à deux semaines de travail tranquille pour une V1 très solide.


####### 16 Juillet 2026

✅ MVP-1 (terminé)

Backend stable.

🎯 MVP-2

Application utilisable par un formateur.

ouvrir une session
afficher le QR
scanner les apprenants
voir les présences en direct
🎯 MVP-3

Administration.

groupes
apprenants
historique
🎯 MVP-4

Exports FSE.

PDF
Excel
statistiques
🎯 v1.0

Application déployable.